document.addEventListener('DOMContentLoaded', function () {
    const targetDiv = document.getElementById('container1');

    // Scroll to the targetDiv with smooth behavior
    targetDiv.scrollIntoView({
        behavior: 'smooth'
    });
});


